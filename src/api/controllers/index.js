const usuariosControlador = require('./usuarios-controlador');
const customersControlador = require('./customers');
const healthControlador = require('./health-controlador');

module.exports = { usuariosControlador, customersControlador, healthControlador };
