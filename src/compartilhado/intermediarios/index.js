const { naoEncontrado } = require('./nao-encontrado');
const { tratadorErro } = require('./tratador-erro');
const { requestId } = require('./request-id');

module.exports = { naoEncontrado, tratadorErro, requestId };
